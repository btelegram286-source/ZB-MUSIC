services:
  - type: web
    name: zb-music-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python reis_bot.py
    envVars:
      - key: BOT_TOKEN
        fromSecret: true
